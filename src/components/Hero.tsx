import { component$ } from "@builder.io/qwik";
import "../styles/Hero.scss";

export default component$(() => {
  return (
    <div>
      <h1>Hola, soy tu portfolio con Qwik 👋</h1>
    </div>
  );
});
